import 'package:flutter/material.dart';

class PeleadoresScreen extends StatelessWidget {
  const PeleadoresScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(
        title: const Text('Peleadores'),
      ),
    );
  }
}